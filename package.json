{
  "name": "oribarilan-website",
  "version": "1.0.0",
  "description": "Personal website",
  "scripts": {
    "start": "npm run build && hugo server",
    "dev": "npm run build && hugo server -D",
    "build": "hugo --gc --minify && npx pagefind --site public --glob '**/*.html' --exclude-selectors '.blogCards,.blogListHeader'"
  },
  "keywords": [],
  "author": "Ori Bar-ilan",
  "license": "MIT",
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
